name: Shell Commands

on: [push]
 
jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: Echo command
        run: echo "hello world"
      - name: multiline script
        run: |
          node -v
          npm -v
